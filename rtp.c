
/*
 * This function will be called when a new RTP packet is to be sent
 */
int send_rtp_packet(char *buffer, int length) {

  // Should typically send to sockeet, i.e.
	// send((SOCKET) socket, buffer, length, flags);
}


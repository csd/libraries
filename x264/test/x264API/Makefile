# Makefile.in generated by automake 1.7.9 from Makefile.am.

srcdir = .
AR = ar
AS = as
CC = gcc
CFLAGS = -g -O2
CPP = gcc -E
CPPFLAGS = 
DEFS = -DHAVE_CONFIG_H
DEPDIR = .deps


SRC = video_codec.c

OBJ = $(SRC:.c=.o)

DEFAULT_INCLUDES =  -I. -I$(srcdir) -I../../
INCLUDES = $(DEFAULT_INCLUDES)

COMPILE = $(CC) $(DEFS) $(DEFAULT_INCLUDES) $(INCLUDES) $(CFLAGS)

all: default

.SUFFIXES:
.SUFFIXES: .c .o

default: libx264api.a

.c.o:
	$(COMPILE) -c $< -o $@

libx264api.a: $(OBJ)
	ar rc libx264api.a $(OBJ)
	ranlib libx264api.a

clean:
	rm -f $(OBJ) libx264api.a
